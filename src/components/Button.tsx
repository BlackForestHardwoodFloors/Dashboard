/**
 * Boardroom 360 Global Button Component
 * 
 * This is the standard button component used throughout the entire application.
 * Import this whenever you need a button.
 * 
 * @example
 * import { Button } from './components/Button';
 * 
 * <Button variant="dashboard">Click Me</Button>
 * <Button variant="success" icon={<Check />}>Save</Button>
 * <Button variant="danger" size="large" fullWidth>Delete</Button>
 */

export { Button, type ButtonProps, type ButtonVariant, type ButtonSize } from './ui/button-plastic';
export { Button as default } from './ui/button-plastic';
